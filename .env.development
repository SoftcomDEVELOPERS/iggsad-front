# Puerto donde corre Vite en dev (opcional, Vite usa 5173 por defecto)
VITE_DEV_PORT=5173

# URL base de la API (Caddy hará proxy a sso-service:5116)
VITE_SSO_URL=http://localhost:5116/api
VITE_COREAPI_URL=http://localhost:5118/api

# URL para websockets o SignalR si las tuvieras
# VITE_WS_URL=wss://localhost:3443/ws

# Nombre de la cookie de auth (sólo como referencia)
VITE_AUTH_COOKIE_NAME=iggsad-auth
